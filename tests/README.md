# tests

Pytest suite covers:

- brief source adapter
- prompt transformer cleanup rules
- retry helper
- end-to-end pipeline state transitions with mocked external providers

Run:

```bash
pytest
```
